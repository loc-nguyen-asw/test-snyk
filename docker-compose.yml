version: '3.9'

services:
  db:
    image: postgres:17-alpine
    container_name: postgres-db
    restart: always
    environment:
      POSTGRES_USER: loc05st
      POSTGRES_PASSWORD: 041001Loc!
      POSTGRES_DB: bum-bi
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
